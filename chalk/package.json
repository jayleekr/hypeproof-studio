{
  "name": "hypeproof-chalk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker — Chalk, the instructor surface for HypeProof Studio (console, cohort reads; the live board lands with plan task G)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --experimental-strip-types test/instructor-console.test.mjs && node --experimental-strip-types test/instructor-auth-drift.test.mjs && node --experimental-strip-types test/board-contract.test.mjs && node --experimental-strip-types test/deploy-isolation.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.34"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.4.0",
    "wrangler": "^4.118.0"
  }
}
