# Studio 실제 작업 체험 — Intent 재설계

상태: 2026-09-08 사용자 피드백 반영. 프로필 초안 구현, 실제 API·앱 관측 및 공개는 미완료.
기존 공개 웹 선택형 체험의 배포를 완료 기준으로 삼지 않는다.

## Intent

고객이 HypeProof Studio에 체험 권한으로 들어와 자기 업무의 작은 일을 실제 AI와
도구로 수행한다. 자신의 목적·맥락·판단이 결과에 어떻게 영향을 주는지 실제
산출물을 비교하여 이해하고, 그 과정에서 관찰된 7자산과 다음 학습 필요를 발견한다.
미션은 ‘AI가 일하는 세상에서, 판단하는 인간을 기른다.’이다.

체험은 Studio 프로그램이다. 웹 설문 점수, 작성된 예시 결과, 정해진 순서의
문제 풀이 완주는 이 Intent의 실현 증거가 아니다. 자유 대화가 어려운 사람에게는
한 번에 한 질문과 구체적인 시작 예시를 주되, 자기 목표를 말하면 이를 따른다.
완료를 위해 정해진 시나리오로 돌아오도록 강제하지 않는다.

## 접근 방식

기본안은 고객별 제한된 참여 코드다. 기존 서명 토큰, 프로필, roster,
session, 만료·폐기 정책을 재사용한다. 공용 bearer 토큰을 공개 페이지나
앱 설치본에 넣지 않는다. 모델 API 키는 기존 Worker에만 둔다.

고객 경로: Studio 설치/실행 → 기존 시작 화면에 코드 입력 → 체험 프로그램 확인
→ 자기 작업 폴더에서 실제 작업 → 산출물 비교와 관찰 결과 → 후속 커리큘럼.
웹의 역할은 설치·신청·사용 안내다. 자동 발급 페이지는 이후 동일한 Service
발급 경로에 연결할 수 있다. 별도 웹 AI 실행기는 첫 구현에 추가하지 않는다.

현재 기존 gate는 활성 session을 요구한다. 따라서 첫 파일럿은 운영자가 연
체험 세션에 개별 고객을 등록한다. ‘언제든 코드 입력만 하면 시작’하는 공개형은
개별 시작 시점·만료·총 사용량·동시 실행 한도를 서버에서 처리한 뒤 제공한다.
profile.session.hours나 모델 max_tokens만으로 총비용 상한이 생겼다고 주장하지 않는다.

## Epic → 요구사항 → 검증

| Epic | 요구사항 | 완료 증거 |
|---|---|---|
| N1 체험 권한 | NAT-01 기존 참여 코드로 전용 프로필을 연결한다. NAT-02 만료/폐기/roster/session/다른 코호트 접근을 기존 gate로 차단한다. | 실제 앱 코드 입력 및 부정 토큰 테스트 |
| N2 자기 업무 실행 | NAT-03 한 번에 한 질문으로 자기 목표를 구체화한다. NAT-04 기존 SDK와 Worker를 통해 실제 API·파일·브라우저 도구를 사용한다. | 공급자 응답, request ID, 실행된 도구 결과, 다시 열린 실제 파일 |
| N3 근거 기반 관찰 | NAT-05 사용자 발화, 코치 제안, 도구 요청, 승인/거절, 실행 성공/실패를 구분한다. NAT-06 별도 평가 API가 원본 event ID와 인용 근거를 반환한다. | 양성/음성/실패/위조 근거 대조군 및 실제 사용자 관측 |
| N4 비교와 발전 | NAT-07 최초 산출물을 보존하고 수정 이유·변경·검증 결과를 비교한다. NAT-08 도움받은 수행과 독립 수행을 구분하고 미관찰은 비워 둔다. | before/after 파일과 검사 재실행, 도움만 받은 사용자의 레벨 불상승 |
| N5 결과와 학습 | NAT-09 Studio 안에서 근거·관찰 범위·다음 행동을 보여준다. NAT-10 커리큘럼 연결은 관찰된 필요를 설명한다. | 앱 화면 및 고객이 자신의 다음 학습 이유를 설명하는 파일럿 |
| N6 공개 운영 | NAT-11 개인별 만료·동시 실행·총 사용량 제한을 서버에서 적용한다. NAT-12 앱/Service 호환성 및 실제 API 장애 경로를 검증한다. | 한도 초과·재시도·동시 요청·구버전·중단 복구 테스트 |

## 측정 설계

실제 API를 호출했다는 사실은 사용량의 증거다. 사람의 자산 성장을 뜻하지 않는다.
자동 관찰과 해석을 아래처럼 분리한다.

| 자산 | 관찰할 근거 | 충분하지 않은 근거 |
|---|---|---|
| TASTE | 대안 사이의 선택과 목적에 맞는 선택 이유 | 예쁜 결과 또는 ‘비교’ 단어 |
| INTENT | 사용자가 정한 목적·대상·완료 조건 | 코치가 대신 작성한 요구사항 |
| CONTEXT | 필요한 자료를 선택·제공하고 부족한 정보를 다룬 행동 | 입력 길이 |
| VERIFY | 검사 결과를 확인하고 오류를 수정·재검증한 행동 | AI의 ‘검증 완료’ 발화 |
| DELEGATE | 맡길 범위·승인 조건·사람이 남길 판단 | 승인 횟수 또는 자동 도구 호출 수 |
| ITERATE | 사용자 피드백 이유와 실제 변경·결과 비교 | 재시도 횟수 |
| OWNERSHIP | 결과 인수 판단, 한계 설명, 재열기·재사용 | 파일이 생성됐다는 사실만 |

평가 모델에는 역할이 분리된 기록을 데이터로 전달한다. 기록 속 명령으로 평가
규칙을 바꿀 수 없어야 한다. 반환한 event ID와 인용문은 서버/호스트에서 대조한다.
도구 실행 사실은 호스트 기록을 근거로 하되 이를 곧바로 사람의 역량으로 치환하지
않는다. 모델의 해석은 잠정 관찰로 표시하고 사람이 수정할 수 있게 한다.
오류·거절·근거 부족은 점수를 만들지 않는다. 1~5 레벨은 검증된 수행 범위에만
부여하며 자동 완주/코칭/반복 횟수로 승급시키지 않는다.

기존 worker/src/lib/asset-scorer.ts는 정규식 기반이며 chat.ts는 AI 응답을 입력한다.
기존 max 누적 % 표시는 이 체험의 측정기로 채택하지 않는다. SDK 경로의 별도
관찰 수집·평가·화면 연결을 구현하기 전에는 ‘실제 7자산 측정 완료’로 출시하지 않는다.

## 레이어와 범위

App: 로컬 도구 실행 근거 수집과 결과 표시. Service: 기존 인증·실행, 평가 API,
사용량 정책. Module: 업무 탐색 코칭과 평가 루브릭. Chalk: 기존 운영자 발급 경로.
Lab: 안내와 커리큘럼 연결. 앱에 평가 임계값이나 직업별 수업 내용을 고정하지 않는다.
초기 프로필은 기존 Profile 계약을 재사용한다. 평가 이벤트 계약은 NAT-05/06 구현
전에 별도로 선언하고 App↔Service drift test와 제거 절차를 함께 작성한다.

원문 서버 보관·강사 열람은 이번 변경으로 켜지 않는다. 평가에 보내는 기록의 범위와
처리 안내를 설계해야 한다. 보관 정책 변경은 AUTONOMY-MANDATE의 별도 결정 사항이다.

## Validation → Intent 루프

프로그램은 실제 업무 선택, 초보자의 시작, 숙련자의 목표 변경, 무비판적 승인,
근거 검증, API 장애, 도구 거절, 코치에 의존한 개선, 새 과제의 독립 수행을 검증한다.
자동 테스트 통과와 고객의 AI 이해 향상은 구분한다. 고객이 ‘내 선택으로 달라진 점’과
‘아직 배워야 할 점’을 자기 말로 설명하지 못하면 코칭·비교 경험과 Intent를 수정한다.

인계 시점에는 전용 프로필과 기존 gate 계약만 있었다. 이후 구현 및 실제 API·Mac
실행 상태는 [실행 기록](../testing/studio-native-trial-results-2026-09-08.md)을 따른다.
공개 배포와 사람의 학습 효과는 별도 검증이며 로컬 성공으로 대신하지 않는다.
실행 환경에 운영/개발 토큰과 공급자 자격증명이 없어 실제 API 검증은 BLOCKED.
프로필을 배포하는 것만으로 위 전체 요구사항이 구현되지는 않는다.

제거: 전용 프로필 등록·프롬프트·해당 테스트를 제거하고 체험 코드 발급을 중지한다.
기존 고객 코호트와 앱 실행 경로에는 별도 마이그레이션을 요구하지 않는다.
