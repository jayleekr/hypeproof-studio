# 큐시트 완주 — 검증 완료 (2026-07-27)

보아치과 큐시트(19:00–21:00)를 **강사가 채팅으로 몰아 끝까지 완주**했다. 결과물
4종이 전부 실재하고, 각각을 코치 말이 아니라 **독립 조회로 확인**했다.

```
앱      v0.1.32
워커     subagents on · 동시성 2 · 부분 읽기 규칙
세션     boah-dental-2026-a / boah-dental-director-copyclone-2026-s1
구간     12:52 ~ 15:00 · 턴 11개 · 55분
```

## 결과물 4종 — 전부 독립 확인

| # | 결과물 | 확인 방법 | 결과 |
|---|---|---|---|
| 1 | 홈페이지 | `~/HypeProofClinic/index.html` | 36,606 bytes |
| 2 | 배포 URL | `curl -sIL https://ico1036.github.io/boa-dental/` | **HTTP 200** · 배포본이 로컬과 바이트 일치 |
| 3 | GitHub 저장소 | `gh api repos/ico1036/boa-dental` | public · 05:54:32Z 생성 |
| 4 | `agent.md` | 로컬·저장소 대조 | 11,538 bytes 일치 |

Pages 는 `build_type=workflow` 다 — 07-26 에 배포를 전원 실패시킨 레거시 브랜치
빌더가 아니다.

## 턴별 실측

```
 턴    소요    코치   승인대기    툴  실패  승인   무엇
  1   241s   239s     2s    14    1    1   커맨드1 (뼈대만)
  2   438s   434s     4s    17    1    1   커맨드1 재실행
  3   453s   453s     0s    52    1    0   커맨드2 콘텐츠 주입 + agent.md
  4    61s    61s     0s     6    1    0   버그 지적 → 1분 수정
  5  1333s  1333s     0s    10    0    0   ⚠️ 22분 멈춤 (실작업 41초)
  6    19s    19s     0s     2    0    0   화면 띄우기
  7    53s    53s     0s     2    0    0   루브릭 정의 (콘텐츠/디자인)
  8   510s   510s     0s    22    0    0   ★ 루프 3바퀴 · 83점 보고
  9    82s    82s     0s     4    0    0   agent.md 갱신 + gh 확인
 10    30s    30s     0s     4    0    0   저장소 생성 + 업로드 + 실재확인
 11    82s    75s     6s    10    2    1   배포 (200 확인 후 발설)

합계 3,303초(55분) · 툴 143 · 실패 6 · 승인 3
```

## 큐시트 블록별

### 19:30–19:45 Reference First — ✓

URL 만 주고 초안이 나왔다. 스크린샷을 요구하지 않았다. 코치가
`browser_open` → `browser_read` 로 정답지를 직접 읽었고, 메뉴를 **실제로
클릭**해가며(`browser_click(e7)`, `(e8)` — 둘 다 첫 시도 성공) 구조를 파악했다.

`"뼈대만"` 한 마디로 산출물이 21.6KB 로 떨어졌다(직전 39.8KB). 크기는 **프롬프트로
제어 가능**하다 — 제품에서 줄 수를 강제할 필요가 없다.

### 19:45–20:05 Context Engineering — ✓

콘텐츠 4종을 `WebFetch`/`browser_read` 로 실제 수집해 `agent.md` 에 저장하고
사이트에 반영했다. `Read` **7회가 전부 범위 지정**이었다:

```
[360~500] [362~392] [191~206] [365~385] [330~366] [390~410] [453~583]
```

전체 읽기가 한 번도 없다. Claude Code 자신이 큰 파일을 다루는 패턴과 같다.

### 20:05–20:35 Loop Engineering — ✓ (커리큘럼의 척추)

강사가 축을 정하고(**콘텐츠 / 디자인**) 코치가 그것을 `agent.md` 체크리스트로
옮겼다 — **루브릭이 참가자 손에서 나왔다.** 그 다음:

> "이 루브릭이 100점 만점에 총 100점 달성할 때까지 확인→수정→확인→수정을 반복해라"

```
+  7~14s   기준(agent.md) + 대상(index.html) 읽기
+   190s   live_preview_start
+259~300s  Edit ×6
+304~323s  screenshot → open → screenshot → browser_read     검사 ①
+352~359s  Edit ×2
+   362s   screenshot                                         검사 ②
+415~437s  Edit ×3
+   492s   Edit(agent.md)  루브릭 갱신
+   496s   screenshot                                         검사 ③
```

**수정 → 검사 를 3바퀴 돌고 루프가 닫혔다.**

### R0 — 통과

100점을 요구받고 **83점이라고 보고했다.**

```
나머지 17점은 실제 자산이 있어야 처리 가능:
  사진(히어로·원장·병원 내부) 10점 · 플로팅 버튼 실제 URL 2점
  원장 Family 섹션 2점 · 태블릿 반응형 4점
```

지어낼 수 있는 자리에서 지어내지 않고 공을 넘겼다. 배포에서도 `curl -sIL` 로
200 을 확인한 **뒤에** 주소를 말했고, 404 구간에서 재배포를 시도하지 않고
12회 폴링으로 기다렸다.

### 20:35–20:58 저장소 · 배포 — ✓

```
git 호출 0회        ← CLT 설치창(2GB) 이 뜨면 회차가 거기서 끝난다
gh api 로 실재 확인  업로드 후 contents 를 다시 조회
Pages build_type=workflow
```

저장소 30초, 배포 82초.

## 이틀 전과 비교

같은 커맨드 2(콘텐츠 주입) 기준:

| | 결과 | 승인 | 툴 | 실패 | 코치 시간 |
|---|---|---|---|---|---|
| v0.1.28 | **20턴 소진으로 2회 사망** | 32회 | 54 | 다수 | — |
| v0.1.29 | 완주 | 16회 | 52 | 2 | 431s |
| v0.1.30 | **산출물 0 (전패)** | 0회 | 40 | 28 | 90s |
| v0.1.31 | 완주 | 0회 | 21 | 0 | 463s |
| v0.1.32 | 완주 | 0회 | 52 | 1 | 453s |

## 남은 것

### 결함 — 22분 멈춤 (턴 5)

실제 작업은 41초에 끝났고 이후 869초 무활동. **워치독도 Stop 도 안 먹었다.**
`Developer: Reload Window` 로 복구된다(원장 확인).

원인 미확정. 승인을 자동 허용으로 바꾸면서 **이벤트가 없는 구간이 길어진 것**이
유력하지만, 스트림이 죽었는데 워치독이 못 잡는 건지 살아 있는데 이벤트만 없는
건지 구분하지 못했다. 재발 시 관측기 기록 + CDP 스트림 상태를 같이 봐야 갈린다.

### 결함 — 루프백 프리뷰 탭이 쌓인다

`coachTabsToClose` 가 루프백을 일부러 제외한다(학생 결과물 창을 지키려는 의도).
그런데 코치가 `#about` `#digital` `#prevent` `#senior` 로 열면 **해시마다 새 탭**이
된다. 지키려던 규칙이 반대로 작동했다. 해시가 달라도 같은 페이지로 봐야 한다.

### 미검증 — 서브에이전트 병렬

v0.1.30 에서 병렬 4개가 `Tool permission request failed: Error: Stream closed` 로
28건 무너졌다. 동시성 2 로 묶은 뒤 **1개만 뜬 상황에서는 정상**이었으나, 병렬 4개
조건이 재현되지 않아 가설은 여전히 미검증이다.

## 이번 회차 운영 메모

- **세션은 `/console` 에서 연다.** `hypeproof-ai.xyz/adult-edu/…` 는 학생용 안내
  페이지라 거기서는 세션이 열리지 않는다. 안 열려 있으면 참가자 전원이
  `403 수업이 아직 시작 전이에요` 를 본다.
- **토큰은 10~12시간짜리로 발급한다.** 오늘 하루에만 두 번 만료로 끊겼다.
- **roster 등록이 토큰 발급보다 먼저다**(#367).
- 우클릭 미리보기는 다음 빌드 전까지 쓰지 않는다 — 라이브 서버와 다른 화면을
  보여 준다(#467 에서 수정).
