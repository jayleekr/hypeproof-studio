# STUDIO_UX_EVAL: 학습 경험 우선 Studio 평가 루브릭

> 작성일 2026-09-18 · 상태: 활성 · 대상: `docs/plan/ux-dag.yaml`의 각 task와 각 단계(P0/P1/P2) PR · 평가자는 구현자와 다른 에이전트다
> 통과선: task 85 / 단계 PR 90. CRITICAL 하나라도 걸리면 점수와 무관하게 실패.

## CRITICAL (하나라도 걸리면 실패)

| ID | 조건 | 확인 방법 |
|---|---|---|
| C1 | 작업 중 화면(스레드 실행 중·에디터 포커스·canvas)에 숫자 점수·등급·퍼센트·배지·"개선 필요"가 있다 | 렌더 DOM 텍스트 덤프에서 정규식 |
| C2 | 금지 라벨(개선 필요, 낮음/높음, 상위 N%, 역량 부족, AI 활용 고수, 성장 점수)이 학생 화면 문자열에 있다 | 웹뷰 소스와 DOM grep |
| C3 | `assetStatusBar` 항목이나 `7자산` 문자열이 빌드에 남아 있다 (P0 이후) | 소스 grep, 상태바 DOM |
| C4 | 새 점수·채점기·저장소가 생겼다 | diff 검토: measurement-core 밖의 새 영속 저장, 새 수치 계산 |
| C5 | AI 작성 텍스트가 actor=student 로 기록된다 | 이벤트 fixture 검사 |
| C6 | 가상/시뮬레이션 데이터가 실제 반응과 같은 라벨·색으로 보인다 | fixture + DOM |
| C7 | 회고가 매 세션 팝업(모달)으로 뜬다 | e2e 흐름 |
| C8 | 테스트 단언을 약화했거나 통과선을 내렸다 | diff 검토 |
| C9 | 6주 내용이 코드 상수로 들어갔다 | 소스 grep: "1주", "Problem / Opportunity" 등 |
| C10 | 요구 문장의 뜻을 바꿨는데 `STATE.md` 개정 제안이 없다 | docs diff |

## 채점 (100)

| 영역 | 점수 | 기준 |
|---|---|---|
| A 계약 충족 | 30 | task가 다루는 SX 행마다 수용 기준을 하나씩 대조. 충족 3 / 부분 1 / 미충족 0, 비율로 환산. 검증 열의 SX-T가 실제로 실행되어 초록(NOT RUN은 0) |
| B 계측기 신뢰 | 15 | control(심어 둔 답)이 구현 전 코드에서 실제로 빨강이었다는 증거. 계측기가 실물(컴포넌트 렌더 분기, 실제 문자열)을 읽었다는 증거. 없으면 0 |
| C 정보 우선순위 | 15 | 학생 홈과 작업 화면에서 현재 과제 > action > 지난번 이어서 > 성장 링크 순서. 5초 회상 테스트 프로토콜을 스크린샷으로 대리 판정: 첫 화면에서 가장 큰 활자가 과제 문장인가 |
| D 코치·개입 | 10 | 코치 응답이 답 대신 질문·기준·힌트로 끝나는가(fixture 대화 5개). intervention ladder 순서. 원문 §10 표의 "나쁜 UX" 문장이 나오면 0 |
| E 근거·출처 | 10 | 해석 문장마다 근거 링크. 기대 조건·이유·전후·출처가 drawer에서 열림. source_state 라벨 |
| F 카피 | 10 | 합니다체, 행동이 주어, 원문 Appendix "쓴다" 열 사용, 영어 섞임 없음(제품명 제외), 평가형 형용사 없음 |
| G 디자인 시스템 | 5 | 토큰 6개 사용, Primary CTA 1개, 상태 = 아이콘 + 문구, 학습 화면에 숫자 카드 없음, 작업물이 주인공(대비 낮춤) |
| H 문서·추적 | 5 | 커밋·PR 본문에 SX ID, 실행한 SX-T와 결과, NOT RUN, 실기 증거 경로. `STATE.md` 갱신 |

## 절차

1. machine_gate 명령을 직접 다시 실행한다. 구현자의 로그를 믿지 않는다.
2. control을 구현 전 트리(`git stash`가 아니라 `git worktree`나 `git archive`)에서 돌려 빨강이었음을 확인한다.
3. CRITICAL 10개를 먼저 본다. 하나라도 걸리면 채점 없이 실패와 이유를 적는다.
4. A~H를 채점하고, 미달 항목마다 구현자가 실행할 수 있는 수정 지시 한 줄을 쓴다.
5. 실패 분류를 남긴다: harness_fixable / harness_undecided / product / spec / flake. 계측기부터 의심한다.
6. 결과를 `.claude/hypeproof/ux/STATE.md` 단계별 게이트 표와 `screens/<task>/judge.md`에 쓴다.
